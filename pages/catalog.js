import React from 'react';
import { Catalog as CatalogLayout } from 'components';

const Catalog = ({ viewportSize }) => (
  <CatalogLayout responsiveSize={viewportSize} />
);

export default Catalog;
